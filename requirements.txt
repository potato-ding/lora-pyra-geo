# Validated Python 3.10 runtime; no model weights or local wheel paths.
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121
torchvision==0.20.1+cu121
deepspeed==0.19.0
numpy==2.2.6
transformers==5.9.0
timm==1.0.27
albumentations==2.0.8
# cv2 without duplicate GUI wheels (server use).
opencv-python-headless==4.13.0.92
Pillow==12.2.0
# Unit-test runner and DeepSpeed extension build tool.
pytest==9.1.1
ninja==1.13.0
